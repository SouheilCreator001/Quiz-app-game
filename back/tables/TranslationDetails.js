/*
const mongoose = require("mongoose");

const CategorieDetailsSchema = new mongoose.Schema({
    arText: String,
    frText: String,
    categorie: {
        type: mongoose.Schema.Types.ObjectId,
        ref: "Categorie"
    }
}, {
    collection: "Translation"
}); 

mongoose.model("Translation", CategorieDetailsSchema);
*/